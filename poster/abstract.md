MiniOpenClaw is a minimal local backend for building persistent, tool-using personal assistant workflows. It combines a sequential task loop with long-lived sessions, workspace-based memory, reusable skills with automatic skill creation, scheduled tasks, and containerized command execution. The system can be used through both a local TUI and Telegram, with both interfaces connected to the same active session. This allows work to continue across interactions while keeping data and execution under local control.